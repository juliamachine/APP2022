{% extends 'base.html.twig' %}

{% block title %}{{ 'category.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'category.new'|trans }}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'action.update'|trans}) }}
    <br>
    <a class="btn btn-secondary" role="button" href="{{ url('app_category_index') }}" title="{{ 'action.back_to_list'|trans }}">
        {{ 'action.back_to_list'|trans }}
    </a>
    <br><br>
    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
