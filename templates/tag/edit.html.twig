{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1>Edit Tag</h1>

    {{ include('tag/_form.html.twig', {'button_label': t('action.update')}) }}
    <br>
    <a class="btn btn-secondary" role="button" href="{{ path('app_tag_index') }}">{{ 'action.back_to_list'|trans }}</a>
    <br><br>
    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
