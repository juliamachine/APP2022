{% extends 'base.html.twig' %}

{% block title %}{{ 'note.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'note.edit'|trans }}</h1>

    {{ include('note/_form.html.twig', {'button_label': 'action.update'|trans}) }}

    <br>

    <a class="btn btn-secondary" role="button" href="{{ url('app_note_index') }}" title="{{ 'action.back_to_list'|trans }}">
        {{ 'action.back_to_list'|trans }}
    </a> <br> <br>

    {{ include('note/_delete_form.html.twig') }}
{% endblock %}
