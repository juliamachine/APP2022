{% extends 'base.html.twig' %}

{% block title %}New Note{% endblock %}

{% block body %}
    <h1>Create new Note</h1>

    {{ include('note/_form.html.twig', {'button_label': t('action.save')}) }}
    <br>
    <a class="btn btn-secondary" role="button" href="{{ path('app_note_index') }}">{{ 'action.back_to_list'|trans }}</a>
{% endblock %}
